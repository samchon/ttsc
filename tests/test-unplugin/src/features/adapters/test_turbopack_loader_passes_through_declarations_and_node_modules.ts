import { assertTurbopackLoaderPassesThroughFilteredPaths } from "../../internal/adapter-turbopack/assertTurbopackLoaderPassesThroughFilteredPaths";

/**
 * Verifies the loader's own filter: declaration files and `node_modules` paths
 * pass through byte-for-byte. A broad `*.ts` rule glob routes everything with
 * the extension through the loader, so the loader must mirror the unplugin
 * adapters' `transformInclude` guard itself.
 */
export async function test_turbopack_loader_passes_through_declarations_and_node_modules(): Promise<void> {
  await assertTurbopackLoaderPassesThroughFilteredPaths();
}
