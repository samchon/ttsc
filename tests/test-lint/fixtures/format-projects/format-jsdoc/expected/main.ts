/**
 * Build a greeting.
 *
 * @param name The recipient.
 * @param greeting The greeting prefix.
 * @returns The combined greeting.
 * @description Legacy description tag.
 */
export function greet(name: string, greeting: string): string {
  return greeting + name;
}
