# Concepts

Two pages of background. Read both before authoring your first plugin.

- [Plugin protocol](/docs/development/concepts/protocol) — the npm package shape, binary subcommands, and diagnostic contract that every `ttsc` plugin implements.
- [AST & Checker](/docs/development/concepts/tsgo) — how TypeScript-Go exposes the AST and Checker to Go plugins. Text ranges, Program bootstrap, type queries.

Then move to [Authoring](/docs/development/authoring).
