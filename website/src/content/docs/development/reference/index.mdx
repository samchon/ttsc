# Reference

Deeper material — read as needed.

- [Architecture](/docs/development/reference/architecture) — source-plugin build cache, cache-key inputs, build environment (`GO_BUILD_ENV_KEYS`, `TTSC_*_BINARY`, `GOROOT`), Go toolchain resolution.
- [Driver API](/docs/development/reference/driver-api) — the Go façade plugin authors should call: `LoadProgram`, `Program`, diagnostics, rewrites, emit, source preambles, LSP-host symbols.
- [Reference plugin tour](/docs/development/reference/reference-plugins) — guided walkthrough of `banner`, `strip`, `paths`, and `lint`, grouped by difficulty. Includes "where the real logic lives" callout for the first-party utility plugins.
- [Pitfalls](/docs/development/reference/pitfalls) — common first-hour failures with exact host error strings (`composes`/`contributors` family, AST panics, Go toolchain).

→ Maintainer-facing workspace and release flow: [Contributing → Workspace release](/docs/contributing/workspace-release).
