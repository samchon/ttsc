# Introduction

![banner of ttsc](/og.jpg)

A TypeScript-Go toolchain. Two packages, one compile pass.

- **`ttsc`** is the compiler. Drop-in for `tsc` — build, check, watch, fix, format.
- **`@ttsc/lint`** is the linter and formatter. 140+ rules. Violations come out as `error TSxxxxx`, indistinguishable from a type error.

Together they replace `tsc + prettier + eslint`. Type errors, lint violations, and format diffs all surface in one `ttsc` run and block the same CI step.

```bash
npm install -D ttsc @ttsc/lint @typescript/native-preview
npx ttsc --noEmit
```

Same `tsconfig.json` you already have. Same flags as `tsc`.

## The shape of the project

| | What |
| -- | ---- |
| `ttsc` | The compiler. Build, type-check, watch, fix, format. |
| `@ttsc/lint` | The linter and formatter. 140 rules. Violations come out as `error TSxxxxx` — same shape as type errors. |
| `ttsx` | TypeScript runner with a real type-check. The typed replacement for `tsx` / `ts-node`. |
| `@ttsc/unplugin` | Same plugin pass embedded inside Vite, Webpack, Rollup, esbuild, Rspack, Rolldown, Farm, Next, and Bun. |
| `typia` / `nestia` | Ecosystem plugins. Runtime validators from your types (`typia`); NestJS controllers + SDK (`nestia`). |

## Reading paths

- **"I just want to use it."** → [Setup](/docs/setup).
- **"I want lint and format as part of my build."** → [@ttsc/lint](/docs/lint). This section is large; the chapter index is the right starting point.
- **"I want to use typia, nestia, or another third-party plugin."** → install the plugin per its own docs (e.g. [typia.io](https://typia.io)); the wiring is one line in `compilerOptions.plugins`, same as any other `ttsc` plugin.
- **"I want a CLI reference."** → [TTSC · `ttsc` CLI reference](/docs/ttsc/compile).
- **"I want to write my own plugin."** → [Plugin Development](/docs/development). Plugin-author territory; consumers don't need it.
- **"Something broke."** → [Troubleshooting](/docs/troubleshooting) or [FAQ](/docs/faq).

## Try it without installing

→ [Playground](/playground)

## Status

v1 and still moving. Install `ttsc` as a normal devDependency. If you publish a library, don't list `ttsc` as a peer dependency yet — keep it build-time only.
