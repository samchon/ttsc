# Rules

A practical rule catalog by category. Each rule maps to an ESLint name where one exists. Severity in `lint.config.ts` is one of `"error"` (fails the build), `"warning"` (prints, exit code 0), `"off"` (skipped).

If you're skimming: the `error`-severity defaults at the bottom of this page are what most projects end up with.

> **Authoritative source:** the canonical rule-name union lives at [`packages/lint/src/structures/TtscLintRule.ts`](https://github.com/samchon/ttsc/tree/master/packages/lint/src/structures/TtscLintRule.ts). This page documents what each rule does and when to keep it on.

## Variable declarations

| Rule | Effect |
| ---- | ------ |
| `no-var` | Reject `var`. Use `let` or `const`. Autofixable. |
| `prefer-const` | When a `let` is never reassigned, demand `const`. Autofixable for single declarations. |

## Cypress

These rules inspect high-confidence Cypress and Mocha test shapes in TS/TSX specs, including `cy.*` command chains and `it` / `before` callbacks.

| Rule | Effect |
| ---- | ------ |
| `cypress/assertion-before-screenshot` | Require a Cypress assertion before `cy.screenshot()`. |
| `cypress/no-and` | Prefer `.should()` over `.and()` when starting Cypress assertion chains. |
| `cypress/no-assigning-return-values` | Reject assigning the return value of Cypress commands. |
| `cypress/no-async-before` | Reject async `before` and `beforeEach` callbacks. |
| `cypress/no-async-tests` | Reject async Cypress test callbacks. |
| `cypress/no-chained-get` | Reject chained `.get()` calls; use `.find()` from the previous subject. |
| `cypress/no-debug` | Reject `cy.debug()` and chained `.debug()` commands. |
| `cypress/no-force` | Reject `{ force: true }` on Cypress action commands. |
| `cypress/no-pause` | Reject `cy.pause()` and chained `.pause()` commands. |
| `cypress/no-unnecessary-waiting` | Reject numeric `cy.wait(...)` sleeps. |
| `cypress/no-xpath` | Reject deprecated `cy.xpath()` selectors. |
| `cypress/require-data-selectors` | Require statically known `cy.get()` selectors to target `data-*` attributes. |
| `cypress/unsafe-to-chain-command` | Reject chaining more commands after Cypress action commands. Option: `{ methods?: string[] }`. |

## Directive comments

These rules inspect `eslint-disable` / `eslint-enable` comments and the matching `lint-disable` aliases supported by `@ttsc/lint`.

| Rule | Effect |
| ---- | ------ |
| `eslint-comments/disable-enable-pair` | Require range `eslint-disable` directives to have a later matching `eslint-enable`. Option: `{ allowWholeFile?: boolean }`. |
| `eslint-comments/no-aggregating-enable` | Reject bare `eslint-enable` comments that re-enable named disables at once. |
| `eslint-comments/no-duplicate-disable` | Reject repeated disables for a rule that is already disabled. |
| `eslint-comments/no-restricted-disable` | Reject disables for configured protected rules. Option: `{ rules: string[] }`. |
| `eslint-comments/no-unlimited-disable` | Reject disable comments with no explicit rule list. |
| `eslint-comments/no-unused-disable` | Reject disable comments that suppress no diagnostic. |
| `eslint-comments/no-unused-enable` | Reject enable comments that do not re-enable anything. |
| `eslint-comments/no-use` | Reject lint directive comments entirely. Option: `{ allow?: string[] }`. |
| `eslint-comments/require-description` | Require directive comments to include a `--` description. |

## Equality and comparison

| Rule | Effect |
| ---- | ------ |
| `eqeqeq` | Reject `==` and `!=`. Autofixable when both operands are typed. |
| `no-self-compare` | Reject `x === x` and friends. |
| `no-self-assign` | Reject `x = x`, including destructured forms. |

## Suggestions

| Rule | Effect |
| ---- | ------ |
| `no-explicit-any` | Reject `any` annotations. Typically `"warning"` during migrations. |
| `prefer-as-const` | Reject `as Literal` when `as const` would do. Autofixable. |
| `object-shorthand` | Reject `{ foo: foo }`. Autofixable. |
| `no-useless-rename` | Reject `{ x: x }` in destructuring. Autofixable. |
| `no-extra-non-null-assertion` | Reject `x!!`. Autofixable. |
| `no-unnecessary-type-constraint` | Reject `<T extends unknown>` and similar. Autofixable. |
| `prefer-namespace-keyword` | Use `namespace` not `module`. Autofixable. |
| `method-signature-style` | Prefer function-property signatures over TypeScript method shorthand signatures. |

## Strings and literals

| Rule | Effect |
| ---- | ------ |
| `no-useless-escape` | Reject `\.` and friends when not required. Autofixable. |
| `no-octal` | Reject octal literals. |
| `no-octal-escape` | Reject `\08`-style escapes. |
| `no-loss-of-precision` | Reject decimal integer literals whose source text cannot round-trip as a JavaScript Number, including overflow-scale values. |
| `no-template-curly-in-string` | Reject `${}` inside non-template strings (probably a bug). |

## Documentation comments

| Rule | Effect |
| ---- | ------ |
| `tsdoc/syntax` | Validate basic TSDoc syntax in `/** ... */` comments, including malformed block tags and malformed or unclosed inline tags. |

## Regular expressions

The first `regexp/*` wave mirrors high-confidence `eslint-plugin-regexp`
checks for regex literals. Dynamic `RegExp("...")` constructor strings are
deferred until the lint engine has a shared literal-value helper.

| Rule | Effect |
| ---- | ------ |
| `regexp/no-control-character` | Reject control characters in regex literals. Alias of the older bare regex check. |
| `regexp/no-dupe-characters-character-class` | Reject duplicate literal characters in simple character classes. |
| `regexp/no-empty-alternative` | Reject empty alternatives such as `/a\|\|b/`. |
| `regexp/no-empty-capturing-group` | Reject empty capturing groups such as `/()/`. |
| `regexp/no-empty-character-class` | Reject empty character classes. Alias of `no-empty-character-class`. |
| `regexp/no-empty-group` | Reject empty non-capturing groups such as `/(?:)/`. |
| `regexp/no-empty-lookarounds-assertion` | Reject empty lookaround assertions such as `/(?=)/`. |
| `regexp/no-misleading-unicode-character` | Reject misleading Unicode characters in character classes. Alias of the older bare misleading-character check. |
| `regexp/no-useless-character-class` | Reject single literal character classes such as `/[x]/`. |
| `regexp/no-useless-escape` | Reject unnecessary regex escapes. Alias of `no-useless-escape` for regex literals. |
| `regexp/no-useless-flag` | Reject flags that do not affect the literal, such as `/\d+/i`. |
| `regexp/no-useless-quantifier` | Reject exact-one quantifiers such as `/a{1}/`. |
| `regexp/no-useless-two-nums-quantifier` | Reject equal min/max quantifiers such as `/a{2,2}/`. |
| `regexp/no-zero-quantifier` | Reject zero-repeat quantifiers such as `/a{0}/`. |
| `regexp/prefer-d` | Prefer `\d` over `[0-9]`. |
| `regexp/prefer-plus-quantifier` | Prefer `+` over `{1,}`. |
| `regexp/prefer-question-quantifier` | Prefer `?` over `{0,1}`. |
| `regexp/prefer-star-quantifier` | Prefer `*` over `{0,}`. |
| `regexp/prefer-w` | Prefer `\w` over `[A-Za-z0-9_]`. |
| `regexp/require-unicode-regexp` | Require the `u` or `v` flag. |
| `regexp/require-unicode-sets-regexp` | Require the `v` flag. |
| `regexp/sort-flags` | Require canonical flag order. |

## Wrapper objects and prototypes

| Rule | Effect |
| ---- | ------ |
| `no-wrapper-object-types` | Reject `String` / `Number` / `Boolean` / `Symbol` / `BigInt`. Autofixable. `Object` stays detection-only. |
| `no-proto` | Reject `obj.__proto__`. |
| `no-prototype-builtins` | Reject `obj.hasOwnProperty(...)`; use `Object.prototype.hasOwnProperty.call`. |

## Loops

| Rule | Effect |
| ---- | ------ |
| `no-constant-condition` | Reject `while (true)` and other constant test expressions. |
| `for-direction` | `for (i = 0; i < 10; i--)` → fail. |
| `prefer-for-of` | Prefer `for..of` when the index is unused. |

## Imports and exports

| Rule | Effect |
| ---- | ------ |
| `no-import-type-side-effects` | Hoist inline `type` modifiers into a single `import type {}`. Autofixable. |
| `consistent-type-imports` | Prefer `import type {}` when imports are types-only. |
| `no-require-imports` | Reject `require(...)` outside CommonJS modules. |
| `no-useless-empty-export` | Reject redundant empty `export {}` declarations in files that already use import/export syntax. |

## Architecture boundaries

These rules are inspired by `eslint-plugin-boundaries`, but `@ttsc/lint` implements a conservative TypeScript source-path subset. They inspect static `import` and `export ... from` module specifiers, resolve relative imports to real source files, and classify files with the rule option's `elements` array.

| Rule | Effect |
| ---- | ------ |
| `boundaries/element-types` | Enforce allowed dependency directions between configured source-path element types. |
| `boundaries/external` | Restrict external package imports by package/specifier pattern. |
| `boundaries/entry-point` | Require imports into an element to target that element's configured public entry files. |
| `boundaries/no-private` | Reject imports of configured private files from outside their element. |
| `boundaries/no-unknown` | Reject relative imports whose resolved source file matches no configured element. |

```ts
// lint.config.ts
export default {
  rules: {
    "boundaries/element-types": ["error", {
      elements: [
        { type: "app", pattern: "src/app/**" },
        { type: "domain", pattern: "src/domain/**", entry: "index.ts", private: "internal/**" },
      ],
      rules: [{ from: "app", disallow: "domain" }],
    }],
    "boundaries/entry-point": ["error", {
      elements: [
        { type: "domain", pattern: "src/domain/**", entry: "index.ts" },
      ],
    }],
    "boundaries/no-private": ["error", {
      elements: [
        { type: "domain", pattern: "src/domain/**", private: "internal/**" },
      ],
    }],
    "boundaries/no-unknown": ["error", {
      elements: [
        { type: "app", pattern: "src/app/**" },
        { type: "domain", pattern: "src/domain/**" },
      ],
    }],
    "boundaries/external": ["error", { disallow: ["@legacy/sdk"] }],
  },
};
```

These diagnostics are not autofixable. A boundary violation usually needs a public API, a dependency inversion, or a config decision; rewriting the import path automatically would be unsafe.

## React Fast Refresh

| Rule | Effect |
| ---- | ------ |
| `react-refresh/only-export-components` | Reject non-component exports in TSX component modules so React Fast Refresh can preserve component state. Supports `extraHOCs`, `allowExportNames`, `allowConstantExport`, and `checkJS` options. |

## Promises and async

| Rule | Effect |
| ---- | ------ |
| `await-thenable` | Reject `await` on non-thenable operands. **Type-aware** — uses the Checker. Autofixable (drops the `await`). |
| `no-async-promise-executor` | Reject `new Promise(async (...) => ...)`. |
| `no-promise-executor-return` | Reject `return` inside a Promise executor. |

## Throw and control flow

| Rule | Effect |
| ---- | ------ |
| `no-throw-literal` | `throw "boom"` → fail. Use `throw new Error(...)`. |
| `no-fallthrough` | Reject `switch` case fall-through without an explicit comment. |
| `no-unsafe-finally` | Reject `return` / `throw` inside a `finally`. |

## Parameters

| Rule | Effect |
| ---- | ------ |
| `default-param-last` | `(req, opt = 1, req2)` → fail. |
| `no-dupe-args` | Function declared with two parameters of the same name. |
| `no-unnecessary-parameter-property-assignment` | Reject `this.x = x` when `x` is already a constructor parameter property. |

## Duplicates

| Rule | Effect |
| ---- | ------ |
| `no-dupe-keys` | `{ a: 1, a: 2 }`. |
| `no-duplicate-case` | Same case label twice in a `switch`. |
| `no-dupe-else-if` | `if (a) ... else if (a) ...`. |

## Miscellaneous

| Rule | Effect |
| ---- | ------ |
| `no-debugger` | Reject `debugger`. |
| `no-empty` | Reject `if (x) {}`, `while (x) {}`, etc. |
| `no-empty-function` | Reject `function f() {}`. |
| `no-with` | Reject `with (...)`. |
| `no-multi-assign` | Reject `a = b = 0` chains. |

## Playwright

These rules mirror high-confidence `eslint-plugin-playwright` checks under the
`playwright/*` namespace. The native implementation focuses on TS test-source
AST patterns that do not require ESLint's scope manager or Playwright runtime
configuration.

| Rule | Effect |
| ---- | ------ |
| `playwright/expect-expect` | Require Playwright test bodies to contain an assertion. |
| `playwright/max-expects` | Limit excessive assertion counts inside one test body. |
| `playwright/no-conditional-expect` | Reject `expect` calls inside conditionals. |
| `playwright/no-conditional-in-test` | Reject conditionals inside Playwright test bodies. |
| `playwright/no-duplicate-hooks` | Reject duplicate Playwright hooks in the same file. |
| `playwright/no-duplicate-slow` | Reject repeated `test.slow()` markers. |
| `playwright/no-element-handle` | Reject direct element-handle APIs such as `$`, `$$`, and `elementHandle`. |
| `playwright/no-eval` | Reject Playwright page and locator evaluation APIs. |
| `playwright/no-focused-test` | Reject `test.only` and `test.describe.only`. |
| `playwright/no-force-option` | Reject `{ force: true }` Playwright action options. |
| `playwright/no-get-by-title` | Prefer stronger locators over `getByTitle`. |
| `playwright/no-hooks` | Reject Playwright hook registration. |
| `playwright/no-nested-step` | Reject nested `test.step()` calls. |
| `playwright/no-networkidle` | Reject `networkidle` load-state and `waitUntil` usage. |
| `playwright/no-nth-methods` | Reject brittle `.first()`, `.last()`, and `.nth()` locator indexing. |
| `playwright/no-page-pause` | Reject committed `page.pause()` debugging calls. |
| `playwright/no-skipped-test` | Reject `test.skip` and `test.describe.skip`. |
| `playwright/no-slowed-test` | Reject `test.slow()` usage. |
| `playwright/no-standalone-expect` | Require `expect` calls to appear inside tests, describes, hooks, or steps. |
| `playwright/no-wait-for-navigation` | Reject `page.waitForNavigation()`. |
| `playwright/no-wait-for-selector` | Reject `page.waitForSelector()`. |
| `playwright/no-wait-for-timeout` | Reject fixed `page.waitForTimeout()` waits. |
| `playwright/prefer-locator` | Prefer locator APIs over direct page selector calls. |
| `playwright/prefer-to-have-count` | Prefer `toHaveCount` over count comparisons. |
| `playwright/prefer-to-have-length` | Prefer `toHaveLength` over length comparisons. |
| `playwright/prefer-web-first-assertions` | Prefer locator/web-first assertions over awaited state plus generic matchers. |
| `playwright/require-to-pass-timeout` | Require an explicit timeout option for `expect(...).toPass()`. |
| `playwright/require-to-throw-message` | Require `toThrow` assertions to include an expected message. |
| `playwright/valid-describe-callback` | Require `test.describe` callbacks to be synchronous. |
| `playwright/valid-expect` | Require `expect` calls to receive an actual value. |
| `playwright/valid-title` | Require Playwright test and describe titles to be non-empty strings. |

## TanStack Query

These rules mirror `@tanstack/eslint-plugin-query` rule names under the
`@tanstack/query/*` namespace. The native implementation uses TypeScript AST
patterns that are stable inside the compiler-integrated lint pass; type-only
edge cases stay conservative.

| Rule | Effect |
| ---- | ------ |
| `@tanstack/query/exhaustive-deps` | Require variables read by `queryFn` to also appear in the local `queryKey` array. |
| `@tanstack/query/infinite-query-property-order` | Require `queryFn` before `getPreviousPageParam` and `getNextPageParam` in infinite query options. |
| `@tanstack/query/mutation-property-order` | Require `onMutate` before `onError` and `onSettled` in mutation options. |
| `@tanstack/query/no-rest-destructuring` | Reject object rest destructuring and spreading over TanStack Query hook results. |
| `@tanstack/query/no-unstable-deps` | Reject whole query or mutation result objects in React dependency arrays. |
| `@tanstack/query/no-void-query-fn` | Reject AST-local `queryFn` callbacks that return no data. |
| `@tanstack/query/prefer-query-options` | Prefer extracting inline `queryKey`/`queryFn` options into reusable query options helpers. |
| `@tanstack/query/stable-query-client` | Reject `new QueryClient()` inside React component or hook bodies. |

## Security

The `security/*` family covers the TypeScript-source-relevant rule surface from `eslint-plugin-security@4.0.0`.

| Rule | Effect |
| ---- | ------ |
| `security/detect-bidi-characters` | Detect Trojan Source bidi control characters. |
| `security/detect-buffer-noassert` | Detect Buffer reads/writes with `noAssert` set to true. |
| `security/detect-child-process` | Detect child_process imports and non-literal `exec` commands. |
| `security/detect-disable-mustache-escape` | Detect object assignments that disable mustache-style escaping. |
| `security/detect-eval-with-expression` | Detect `eval` calls fed by non-literal expressions. |
| `security/detect-new-buffer` | Detect `new Buffer` with non-literal input. |
| `security/detect-no-csrf-before-method-override` | Detect Express csrf middleware configured before methodOverride. |
| `security/detect-non-literal-fs-filename` | Detect filesystem APIs with non-literal filename arguments. |
| `security/detect-non-literal-regexp` | Detect RegExp construction from non-literal patterns. |
| `security/detect-non-literal-require` | Detect `require` calls with non-literal module specifiers. |
| `security/detect-object-injection` | Detect dynamic bracket access sinks. |
| `security/detect-possible-timing-attacks` | Detect direct equality comparisons involving secret-like identifiers. |
| `security/detect-pseudoRandomBytes` | Detect `crypto.pseudoRandomBytes`. |
| `security/detect-unsafe-regex` | Detect high-confidence catastrophic backtracking regex shapes. |

## Format

The `format*` rules cover Prettier's territory. They live on their own page — [Format rules](/docs/lint/format).

## A common starter set

Most projects start here. Add and remove as you go.

```ts
// lint.config.ts
import type { ITtscLintConfig } from "@ttsc/lint";

export default {
  rules: {
    // Variables
    "no-var": "error",
    "prefer-const": "error",

    // Comparison
    "eqeqeq": "error",
    "no-self-compare": "error",

    // Suggestions
    "no-explicit-any": "warning",
    "prefer-as-const": "error",
    "object-shorthand": "error",
    "no-useless-rename": "error",

    // Imports
    "no-import-type-side-effects": "error",
    "consistent-type-imports": "error",

    // Misc safety
    "no-debugger": "error",
    "no-throw-literal": "error",
    "await-thenable": "error",

    // Format (see /docs/lint/format)
    "format/semi": "error",
    "format/quotes": ["error", { prefer: "double" }],
    "format/trailing-comma": "error",
    "format/print-width": ["error", { printWidth: 80 }],
  },
} satisfies ITtscLintConfig;
```

## Autofix coverage at a glance

`ttsc fix` autofixes a subset. Native fixers cover:

- `no-var`, single-declaration `prefer-const`, ESLint-safe `eqeqeq`
- `no-wrapper-object-types` (primitives only; `Object` is detection-only)
- `prefer-as-const`, `no-useless-rename`, `object-shorthand`, `no-extra-non-null-assertion`
- `no-unnecessary-type-constraint`, `prefer-namespace-keyword`, `no-useless-escape`
- `no-import-type-side-effects` (hoists inline `type` modifiers)
- `await-thenable` (type-aware — drops `await` on non-thenable operands)
- Every format rule

## See also

- [Format](/docs/lint/format) — the format rule set (including `format/print-width`).
- [Overview](/docs/lint) — the `ttsc fix` / `ttsc format` command summary.
