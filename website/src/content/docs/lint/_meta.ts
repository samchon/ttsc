import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  setup: "Setup",
  rules: "Rules",
  format: "Format rules",
  fix: "Fix & format commands",
};
export default meta;
