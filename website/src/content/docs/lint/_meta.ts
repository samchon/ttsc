import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  setup: "Setup",
  editor: "VS Code",
  videos: "Video library",
  format: "Format",
  rules: "Rules",
};
export default meta;
