{
  "requiredSymbols": [
    "track",
    "Dep.track",
    "trigger",
    "Dep.notify",
    "ReactiveEffect.run"
  ],
  "requiredEdges": [
    ["track", "Dep.track", "calls"],
    ["Dep.trigger", "Dep.notify", "calls"],
    ["Dep.notify", "ReactiveEffect.notify", "calls"]
  ],
  "mustMention": ["dependency", "effect", "subscriber", "batch"],
  "mustNotClaim": ["Vue uses a Set to store effect subscribers per dependency"]
}
