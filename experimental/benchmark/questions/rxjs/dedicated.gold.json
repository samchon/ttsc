{
  "requiredSymbols": [
    "Observable.pipe",
    "pipeReducer",
    "Observable.subscribe",
    "Observable._trySubscribe",
    "operate"
  ],
  "requiredEdges": [
    ["Observable.pipe", "pipeReducer", "calls"],
    ["Observable.subscribe", "Observable._trySubscribe", "calls"],
    ["operate", "Subscriber", "calls"]
  ],
  "mustMention": ["subscriber", "operator", "_subscribe", "destination"],
  "mustNotClaim": ["pipe eagerly executes operators at composition time rather than at subscribe time"]
}
