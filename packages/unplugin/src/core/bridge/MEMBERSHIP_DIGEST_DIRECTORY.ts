/**
 * The directory below a host's tool directory that holds the membership
 * records, one per project (`membershipDigestFile`), which a build start
 * enumerates to refresh them (`refreshMembershipDigestFiles`).
 */
export const MEMBERSHIP_DIGEST_DIRECTORY = "membership";
