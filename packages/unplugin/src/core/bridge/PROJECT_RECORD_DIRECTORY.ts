/**
 * The directory below a host's tool directory (`hostToolDirectory`) that holds
 * one project record per project (`projectRecordFile`): the one file every
 * module of the project depends on, in every host's own channel and in its
 * persistent cache.
 */
export const PROJECT_RECORD_DIRECTORY = "records";
