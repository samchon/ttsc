Natural-language call-flow discovery. Use this for lifecycle, rendering, dispatch, validation, I/O, or "how does X reach Y" tasks when exact endpoints are not known.

It ranks likely declarations from the task terms, expands the relevant value-call/value-access flow, and returns compact source windows around runtime evidence plus edge evidence in one result. Prefer it before chaining broad `query_nodes` calls.
