Project-wide exported TypeScript symbol index. In a fresh project session, use this once before the first query_nodes call unless you already have exact handles from a prior graph result.

Returns exported classes, interfaces, types, enums, functions, variables, and exported members known to the compiler, with kind, file:line, handle, export source, folder summary, and relationship counts. Git-ignored generated files are omitted from this orientation index.
