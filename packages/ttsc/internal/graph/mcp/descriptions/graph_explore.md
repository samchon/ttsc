Call once first for code-flow questions, instead of shell/grep/read. One broad query (owner/action/nouns together, e.g. "repository find manager query builder") returns the compiler-resolved TypeScript graph snapshot: source, calls, callers, types, and blast radius. Answer the whole flow from the result: edge targets are already part of the path, so do not re-query just to expand a node you can already see, and do not shell/grep/read paths it printed. The graph reflects the code as last built, so it changes when the source changes; re-query after you edit a file, since an earlier snapshot can be stale.
