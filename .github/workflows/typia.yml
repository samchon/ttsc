name: typia

on:
  pull_request:
    paths:
      - ".github/workflows/typia.yml"
      - "experimental/**"
      - "scripts/**"
      - "packages/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
  workflow_dispatch:

concurrency:
  group: typia-${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      # Only build + pack the current platform (ttsc-linux-x64) — typia's
      # overrides below pull in ttsc.tgz + ttsc-linux-x64.tgz only. The full
      # cross-platform matrix takes ~20 min for tarballs we never consume.
      TTSC_TARBALLS_CURRENT: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.6.4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.x
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - uses: actions/setup-go@v5
        with:
          go-version-file: packages/ttsc/go.mod
          cache-dependency-path: |
            packages/ttsc/go.sum
            tests/go-transformer/go.mod

      - name: Install ttsc dependencies
        run: pnpm install --frozen-lockfile

      - name: Build ttsc tarballs (current platform only)
        run: pnpm package:tgz

      - name: Clone typia next
        run: |
          rm -rf experimental/typia
          # TEMP: clone the native-ttsx integration branch while it is in flight.
          # Switch back to master (drop --branch) at final merge.
          git clone --depth=1 --branch feat/native-ttsx-integration https://github.com/samchon/typia experimental/typia

      - name: Use local ttsc tarballs
        working-directory: experimental/typia
        run: |
          # typia's pnpm-workspace.yaml already declares its own `overrides:`
          # block (and resolves ttsc through a catalog), so appending a second
          # `overrides:` key makes pnpm fail with a duplicated-mapping-key YAML
          # error. Merge the local-tarball overrides into the existing block
          # instead, creating the block only when typia drops it.
          node -e '
          const fs = require("fs");
          const file = "pnpm-workspace.yaml";
          const entries = [
            "  ttsc: file:../tarballs/ttsc.tgz",
            "  \"@ttsc/linux-x64\": file:../tarballs/ttsc-linux-x64.tgz",
          ].join("\n");
          const text = fs.readFileSync(file, "utf8");
          if (text.includes("file:../tarballs/ttsc.tgz")) process.exit(0);
          const merged = /^overrides:[ \t]*$/m.test(text)
            ? text.replace(/^overrides:[ \t]*$/m, (line) => line + "\n" + entries)
            : text.replace(/\s*$/, "") + "\n\noverrides:\n" + entries + "\n";
          fs.writeFileSync(file, merged);
          '

      # 시간의 ~85%는 첫 ttsx에서 cmd/ttsc-typia(=tsgo+ttsc+typia native)를
      # 콜드 컴파일하는 데 쓰인다. 전용 GOCACHE와 플러그인 바이너리 캐시를 run
      # 간 보존해, 소스 미변경 PR은 풀 스킵(수 초), 소스 변경 PR도 restore-keys로
      # GOCACHE가 warm이라 증분 빌드가 된다. typia native go 소스 해시로 키를 잡아
      # 소스가 바뀐 PR에서만 정확-키가 무효화된다.
      - name: Cache typia native go-build + ttsc plugin binaries
        uses: actions/cache@v4
        with:
          path: |
            ${{ runner.temp }}/typia-gocache
            ${{ runner.temp }}/ttsc-plugin-cache
          key: typia-native-${{ runner.os }}-${{ hashFiles('experimental/typia/packages/typia/native/**/*.go', 'experimental/typia/packages/typia/native/go.sum') }}
          restore-keys: |
            typia-native-${{ runner.os }}-

      - name: Test typia next
        working-directory: experimental/typia
        env:
          # buildSourcePlugin은 process.env의 GOCACHE를 그대로 go build에 넘기고
          # (goBuildEnv), TTSC_CACHE_DIR를 플러그인 바이너리 캐시 루트로 읽는다.
          GOCACHE: ${{ runner.temp }}/typia-gocache
          TTSC_CACHE_DIR: ${{ runner.temp }}/ttsc-plugin-cache
        run: |
          pnpm install --no-frozen-lockfile
          pnpm run test
