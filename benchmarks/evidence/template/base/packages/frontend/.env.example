# Simulation is selected by the Vite mode, never here. A value in `.env` is
# promoted into the process environment before Vite resolves a mode, so it would
# govern every build including the live gate. Use `--mode contract` instead.
VITE_API_HOST=http://127.0.0.1:37001
